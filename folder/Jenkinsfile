pipeline {
    agent any

    stages{
        stage('sonarqube analysis'){
            steps{
                nodejs(nodeJSInstallationName: 'nodejs'){
                    sh 'npm install'
                    withSonarQubeEnv('sonar'){
                        sh 'npm run sonar'
                    }
                }
            }
        }
    }
}