pipeline{
    agent{
        label 'ubuntu-server'
    }

    stages{
        stage('Run nodejs container'){
            agent{
                docker{
                    image 'nodejs'
                    args '-u root:root'
                }
            }
            steps{
                sh 'npm install'
            }
        }
    }
}