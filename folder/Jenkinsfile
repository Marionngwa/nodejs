pipeline {
    agent any

    stages {
        stage('Install Dependencies') {
            agent {
                docker {
                    image 'node:14'
                    args '-u root:root'
                }
            }
            steps {
                sh 'npm install'
            }
        }
    }
}
