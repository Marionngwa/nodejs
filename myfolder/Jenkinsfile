pipeline {
    agent any
    environment {
        PATH = "/opt/node/v14.21.3/bin:$PATH"
    }
    stages {
        stage('dependencies') {
            steps {
                sh 'npm install'
            }
        }
        stage('start the app'){
            steps {
                sh 'npm pack'
            }
        }
        
    }
}
